/** @format */

const contractAddress = require("./contractAddress.json");
const abi = require("./abi.json");
const NFTcontractAddress = require("./NFTcontractAddress.json");
const NFT_ABI = require("./nftABI.json");

module.exports = { contractAddress, abi, NFTcontractAddress, NFT_ABI };
